class @<%= view_name.camelize %> extends egg.View
  
  @className: '<%= underscore_name %>'

  @init (opts)->
    @render()
  
  render: ->
    $(@elem).html JST['<%= underscore_name %>'](@presenter().toJSON())
