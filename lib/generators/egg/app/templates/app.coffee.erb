#= require egg
#= require mustache
#= require_tree ../../shared
#= require_tree ./observers

class @<%= app_name.camelize %> extends egg.View

  @init (opts)->
    @server = egg.RestApi.create(headers: {'X-CSRF-Token': opts.csrfToken})
    
    # Kick off your app here!
    alert("Welcome to my awesome <%= app_name.camelize %>!")
