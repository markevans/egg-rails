# TODO: where does "app" come from?????
define ['models/<%= model_name %>'], (<%= model_name.camelize %>)->
  <%= model_name.camelize %>.on

    # commit: (params)->
    #   app.server.save(params.instance)

    # destroy: (params)->
    #   app.server.destroy(params.instance)
